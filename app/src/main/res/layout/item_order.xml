<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <TextView
        android:id="@+id/orderIdTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Order ID: #123456"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/orderDateTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/orderIdTextView"
        android:layout_marginTop="4dp"
        android:text="Order Date: June 1, 2023"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/orderTotalPriceTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/orderDateTextView"
        android:layout_marginTop="4dp"
        android:text="Total: $50.00"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/taxTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/orderTotalPriceTextView"
        android:layout_marginTop="4dp"
        android:text="Total: $50.00"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/shippingAddressTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/taxTextView"
        android:layout_marginTop="4dp"
        android:text="Total: $50.00"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/orderNumberTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/shippingAddressTextView"
        android:layout_marginTop="4dp"
        android:text="Total: $50.00"
        android:textSize="14sp" />

    <!-- Add more views for other order details as needed -->

</RelativeLayout>
